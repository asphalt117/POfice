@model IEnumerable<Domain.Entities.CustomInfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cod1s)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Txt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isEmpty)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cod1s)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Txt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isEmpty)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CustomInfoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CustomInfoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CustomInfoId })
        </td>
    </tr>
}

</table>
